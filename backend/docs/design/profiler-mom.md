Продаваемые поинты (майлстоуны):
- Реанимировать CDT (профайлер стек) с текущими стораджами.
  - Выкинуть постгрю???
- MCP - и тут выдавать
- Добавить UI для dumps-collector ? - есть PV, можно сделать SKILL troubleshooting для вытаскивания дампов без UI ???
- Skill на скачивание и поиск UI дерева ???
- Починить UI (запускать через агента) ???

0) Надо заложить багофикс
1) Завести CDT бекенд
  - collector (со стораджем)
  - maintenance
  - query (UI)
1.1) Проблема с UI (при публикации отказались от NC либ и надо перевязать к AntDesign)
1.2) Maintenance - Оптимизация ресурсов? (RAM прожорливый)
1.3) Добить миграцию на новую монорепу (packages import ведёт на заархивленный репозиторий)
2) Оптимизации:
  - Фильтрация на уровне агента (целиком call-tree выполняется быстрее порога - то исключать весь call-tree) ???
  - Разные политики ротации по duration
3) Архитектура микросервисная: пересмотреть архитектуру и возможно упростить её (сейчас Postgres + Cassandra + S3 + PV).
  - Предложение - сделать all-in-one приложение (хранилище ???) - чтобы хранились в одном PV (отказаться от 3rd party баз, оставить PV и опционально S3). Один бинарник - чтобы из одной поды иметь RWO PV. Плюс пользователю удобнее запускать на Dev.
  - Проанализировать формат данных для долгосрочного (паркеты) и краткосрочного хранения (in-memory агрегация и swap на диск)
    - Уточнить что пишется в Postgres???
  - Разделение на два приложения dumps-collector + профайлер (collector + ui)
4) Выгрузить имеющиеся дизайны из Figma
  - Проанализировать новые view/UI
    - CallTree на потом
  - Аналитика/метрики/Dashboard UI (в Maintenance джобе? или в самом агенте?).
5) Применение конфигурации в Runtime в агенте и централизованая доставка конфигов (через UI/Параметры - надо продумать)
6) MCP/Skill на скачивание треддампа (gc логов, может быть Heap Dumps) через collector
  - MCP/Skill для работы с диагностическими данными (через collector или kubectl???)
  - Не забывать слова Панкратова про хороший машинночитаемый API
  